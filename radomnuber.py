import random # imports random in python

from random import randint # imports random from randint

randomnumber = random.randint(1,100000) # selects one number between 1 and 100,000!

# it also stores the random number in a variable called  random number

print (randomnumber) # prints the random number